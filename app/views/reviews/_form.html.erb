<%= form_for([@music, @review]) do |f| %>
  <% if @review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% @review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <strong>Оценка</strong><div id="star-rating"></div>
  </div>
  <div class="field">
    <%= f.label "Текст комментария" %><br>
    <%= f.text_area :comment, class: "form-control" %>
  </div>
  <br/>
  <div class="actions">
    <%= f.submit "Создать обзор", class: 'btn btn-primary btn-lg' %>
  </div>
<% end %>

<script>
  $('#star-rating').raty({
    path: '/assets/',
    scoreName: 'review[rating]'
  });
</script>
